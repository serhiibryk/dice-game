import { Stack } from '@mui/material';
import { styled } from '@mui/material/styles';

export const StyledSliderWrapper = styled(Stack)(({ theme }) => ({
  marginTop: theme.spacing(4),
  marginBottom: theme.spacing(2),
}));
